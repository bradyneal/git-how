EDITING CONFIG FILE:
Set name (not related to GitHub) - git config --global user.name "[full name]"
Set email (attached to GitHub) - git config --global user.email "[email address]"
Enable helpful colorization - git config --global color.ui auto
Location of git config file - ~/.gitconfig
